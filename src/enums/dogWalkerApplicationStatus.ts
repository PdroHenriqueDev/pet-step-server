export enum DogWalkerApplicationStatus {
  PendingDocuments = 'pendingDocuments',
  PendingReview = 'pendingReview',
  Approved = 'approved',
  Rejected = 'rejected',
  Deactivated = 'deactivated',
}

export enum DocumentReviewStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
}
